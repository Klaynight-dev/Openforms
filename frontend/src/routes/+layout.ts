// Application rendue côté client (SPA) : simplifie l'authentification par
// cookies cross-origin avec l'API Elysia et évite tout fetch SSR.
export const ssr = false;
export const prerender = false;
