{
  "name": "@humanitour/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Serveur MCP exposant les formulaires, statistiques et presets d'Openforms à un client compatible (Claude Desktop, etc.).",
  "bin": {
    "openforms-mcp": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
